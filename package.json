{
  "name": "unit_test",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "test": "jest",
    "coveralls": "jest --coverage && cat ./coverage/lcov.info | coveralls"
  },
  "keywords": [
    "MDS",
    "Project",
    "unit",
    "tests"
  ],
  "author": "MDS",
  "devDependencies": {
    "jest": "^24.9.0"
  },
  "dependencies": {
    "axios": "^0.19.1",
    "save-dev": "0.0.1-security"
  }
}
